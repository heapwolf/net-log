{
  "name": "net-log",
  "version": "0.2.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "test": "mocha --recursive test --bail --colors --reporter spec --debug -u tdd --log true"
  },
  "author": "hij1nx",
  "license": "MIT",
  "dependencies": {
    "json-stringify-safe": "^5.0.0",
    "monotonic-timestamp": "0.0.8"
  },
  "devDependencies": {
    "level": "^0.18.0",
    "mocha": "^1.18.2"
  }
}
